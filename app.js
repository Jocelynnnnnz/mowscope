STORIES.sort((a,b)=>b.date.localeCompare(a.date));const kinds=["全部资讯","国内媒体","公众号精选","海外媒体","品牌新闻","机器人产业"];const filters=document.querySelector("#filters"),grid=document.querySelector("#stories");
function render(active="全部资讯"){filters.innerHTML=kinds.map(k=>`<button class="${k===active?"active":""}" data-kind="${k}">${k}<span>${k==="全部资讯"?STORIES.length:STORIES.filter(s=>s.kind===k).length}</span></button>`).join("");const list=active==="全部资讯"?STORIES:STORIES.filter(s=>s.kind===active);grid.innerHTML=list.map((s,i)=>`<article class="story"><div class="story-meta"><span>${s.market}</span><span>${s.date}</span></div><span class="story-tag">${s.kind}</span><h3>${s.title}</h3><p>${s.summary}</p><a href="${s.url}" target="_blank" rel="noopener"><span>${s.source}</span><b>阅读原文 →</b></a></article>`).join("");filters.querySelectorAll("button").forEach(b=>b.onclick=()=>render(b.dataset.kind))}
render();
document.querySelector("#policies").innerHTML=POLICIES.map((p,i)=>`<a class="policy-card" href="policy.html?policy=${p.slug}"><div class="policy-card-top"><span>${p.region}</span><b>${p.status}</b></div><small>${String(i+1).padStart(2,"0")} / POLICY BRIEF</small><h3>${p.title}</h3><p>${p.summary}</p><footer><span>${p.date}</span><b>查看解读 →</b></footer></a>`).join("");
document.querySelector("#services").innerHTML=SERVICES.map(s=>`<a class="service" href="research.html?service=${s.slug}"><span>${s.num}</span><div><h3>${s.title}</h3><p>${s.short}</p><small>${s.timeline}</small></div><b>查看详情 →</b></a>`).join("");